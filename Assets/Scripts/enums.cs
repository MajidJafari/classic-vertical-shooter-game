namespace Pool
{
    public enum ObjectTypes
    {
        Shot,
        Alien,
        AlienShot
    }
}

namespace State
{
    public enum States
    {
        TitleMenu,
        Loading,
        GamePlay,
        Dying,
        GameOver,
        NextLevel
    }
}
